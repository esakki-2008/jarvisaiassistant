name: Build JARVIS Android APK

on:
  workflow_dispatch:
  push:
    paths:
      - "android-companion/**"
      - ".github/workflows/android-apk.yml"

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: android-companion
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up JDK 17
        uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle

      - name: Set up Android SDK
        uses: android-actions/setup-android@v4
        with:
          packages: "platform-tools"

      - name: Install Android SDK packages
        run: sdkmanager "platforms;android-35" "build-tools;35.0.0"

      - name: Generate Gradle wrapper
        run: gradle wrapper --gradle-version 8.10.2

      - name: Clean project
        run: ./gradlew clean

      - name: Run lint
        run: ./gradlew lintRelease --warning-mode all

      - name: Build release APK
        run: ./gradlew assembleRelease --warning-mode all

      - name: Sign release APK
        shell: bash
        run: |
          keytool -genkeypair -v             -keystore jarvis-release.keystore             -alias jarvis             -keyalg RSA             -keysize 2048             -validity 10000             -storepass jarvis-temp-password             -keypass jarvis-temp-password             -dname "CN=JARVIS, OU=Mobile, O=Esakki Raja, L=India, C=IN"
          apksigner sign             --ks jarvis-release.keystore             --ks-key-alias jarvis             --ks-pass pass:jarvis-temp-password             --key-pass pass:jarvis-temp-password             app/build/outputs/apk/release/app-release.apk
          apksigner verify --verbose app/build/outputs/apk/release/app-release.apk

      - name: Locate release APK
        id: locate_apk
        shell: bash
        run: |
          apk="$(find app/build/outputs -type f -name '*.apk' -size +0c -print -quit)"
          test -n "$apk"
          echo "apk=$apk" >> "$GITHUB_OUTPUT"
          echo "APK found: $apk"
          ls -lh "$apk"

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: jarvis-companion-release
          path: android-companion/${{ steps.locate_apk.outputs.apk }}
          if-no-files-found: error
